# 依赖
node_modules/
.pnpm-store/

# 打包产物（由 scripts/package.bat 生成）
dist/
out/

# 便携运行时（手动放入，体积大，不进 git）
vendor/

# 本地环境变量 / 运行时标记
.env
.env.local
.initialized

# 系统
.DS_Store
Thumbs.db

# 运行时生成的数据（本地开发用，不进 git）
packages/cloud-license-service/data/
packages/local-server/backups/
packages/local-server/src/data/
_check.mjs

